"""Per-category MCP tool schema definitions.

Split out of the former monolithic schemas/tool_schemas.py; each module
exposes one ``*_TOOLS`` list. schemas/tool_schemas.py re-aggregates them
into ``TOOL_SCHEMAS`` and re-exports every list for backward compatibility.
"""
