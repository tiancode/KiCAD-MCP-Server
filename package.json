{
  "name": "kicad-mcp",
  "version": "2.2.3",
  "description": "AI-assisted PCB design with KiCAD via Model Context Protocol",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "node dist/index.js",
    "dev": "npm run build:watch & nodemon dist/index.js",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "test": "npm run test:ts && npm run test:py",
    "test:ts": "tsc --noEmit",
    "test:py": "node scripts/run-py.mjs -m pytest tests/ -v",
    "test:coverage": "node scripts/run-py.mjs -m pytest tests/ --cov=python --cov-report=html --cov-report=term",
    "lint": "npm run lint:ts && npm run lint:py",
    "lint:ts": "eslint src/",
    "lint:py": "node scripts/run-py.mjs -m black --check python && node scripts/run-py.mjs -m mypy python && node scripts/run-py.mjs -m flake8 python",
    "format": "prettier --write 'src/**/*.ts' && node scripts/run-py.mjs -m black python",
    "prepare": "npm run build",
    "pretest": "npm run build"
  },
  "keywords": [
    "kicad",
    "mcp",
    "model-context-protocol",
    "pcb-design",
    "ai",
    "claude"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "dotenv": "^17.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "@eslint/js": "^10.0.1",
    "@types/glob": "^8.1.0",
    "@types/node": "^20.19.0",
    "eslint": "^10.1.0",
    "nodemon": "^3.0.1",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.2"
  }
}
